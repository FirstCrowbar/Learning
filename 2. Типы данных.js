const persone = '5';        /* Строка */

const bool = false;         /* Булевое значение */

const obj = {               /* Объект, содержит в себе один и более элементов */
    name: "John",
    age: 25,
    isMarried: false
};
console.log(obj.isMarried); /* Пример обращения к одному из элементов объекта */

let arr = ['plum.png', 'orange.jpg', 'apple.bmp']; /* Массив */
console.log(arr[0]);                               /* Пример обращения к первому элементу в массиве */
